import type {Metadata} from "next"
import Image from "next/image"
import Link from "next/link"
import {StoreLayout} from "@/components/storefront/store-layout"
import {families,products} from "@/data/products"
const descriptions={Floral:"Petal-led, expressive and luminous.",Woody:"Warm, grounded and quietly confident.",Amber:"Golden, enveloping and richly textured.",Oud:"Deep, polished woods with sensual warmth.",Citrus:"Bright, aromatic and full of refined energy.",Fresh:"Transparent, clean and easy to wear."}
export const metadata:Metadata={title:"Fragrance Families | Aurévia",description:"Explore the Floral, Woody, Amber, Oud, Citrus and Fresh families in the Aurévia catalog."}
export default function Families(){return <StoreLayout><main className="aurevia-container py-16"><nav aria-label="Breadcrumb" className="text-sm text-muted-foreground"><Link href="/home">Home</Link> / <Link href="/fragrance-guide">Fragrance Guide</Link> / Families</nav><p className="text-eyebrow mt-10 text-aurevia-gold">Fragrance guide</p><h1 className="text-display mt-3 text-primary">Find your fragrance family.</h1><div className="mt-12 grid gap-7 md:grid-cols-2">{families.map(f=>{const matches=products.filter(p=>p.fragranceFamily===f),visual=matches.find(p=>Object.keys(p.images).length>1)??matches[0];const notes=[...new Set(matches.flatMap(p=>[...p.topNotes,...p.heartNotes,...p.baseNotes]))].slice(0,3);return <article key={f} className="grid overflow-hidden border sm:grid-cols-[12rem_1fr]"><div className="relative min-h-56"><Image src={visual.images.editorial??visual.images.ingredient??visual.image} alt={`${f} fragrance family`} fill sizes="(max-width:640px) 100vw,200px" className="object-cover"/></div><div className="p-6"><h2 className="font-display text-4xl text-primary">{f}</h2><p className="mt-2 text-muted-foreground">{descriptions[f]}</p><p className="mt-4 text-sm"><strong>Common notes:</strong> {notes.join(" • ")}</p><p className="mt-2 text-sm"><strong>Explore:</strong> {matches.slice(0,3).map(x=>x.name).join(", ")}</p><Link href={`/shop?family=${f.toLowerCase()}`} className="mt-5 inline-block font-semibold text-primary underline">Shop {f}</Link></div></article>})}</div></main></StoreLayout>}
