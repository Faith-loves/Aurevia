"use client"
import Link from "next/link"
import { ProductCard } from "@/components/storefront/product-card"
import { Button } from "@/components/ui/button"
import { products } from "@/data/products"
import { useWishlistStore } from "@/store/wishlist-store"
export function WishlistContent(){const slugs=useWishlistStore(s=>s.slugs),items=slugs.map(slug=>products.find(p=>p.slug===slug)).filter((p):p is (typeof products)[number]=>Boolean(p));return <>{items.length?<><div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-eyebrow text-aurevia-gold">Saved scents</p><h1 className="text-display mt-3 text-primary">Your Wishlist</h1><p className="mt-3 text-muted-foreground">{items.length} saved {items.length===1?"fragrance":"fragrances"}</p></div><p className="text-sm text-muted-foreground">Sign in to keep your favorites across devices. <Link href="/sign-in" className="font-semibold text-primary underline">Sign In</Link></p></div><div className="mt-10 grid grid-cols-2 gap-x-4 gap-y-10 md:grid-cols-3 lg:grid-cols-4">{items.map(p=><div key={p.id}><ProductCard product={p}/><Button asChild variant="outline" className="mt-4 w-full"><Link href={`/product/${p.slug}`}>Choose Options</Link></Button></div>)}</div></>:<div className="flex min-h-[55vh] items-center justify-center text-center"><div><p className="text-eyebrow text-aurevia-gold">Saved scents</p><h1 className="text-display mt-3 text-primary">Your wishlist is waiting.</h1><p className="mt-4 text-muted-foreground">Save the fragrances you want to come back to.</p><Button asChild className="mt-7"><Link href="/shop">Explore Fragrances</Link></Button></div></div>}</>}
